# Ingestion Invalid Fixtures

These fixtures are intentionally small examples for Milestone 3 ingestion tests and manual debugging. They are not part of the committed valid NexaFlow sample.

- `malformed_events.jsonl`: malformed JSON, non-object JSON, and an event with an unknown event name.
